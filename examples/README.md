# Example output

## `Flaskr-Walkthrough.html`

A complete walkthrough generated by the `generate-walkthrough` skill from the
**Flask tutorial application (`flaskr`)**:

- Source: <https://github.com/pallets/flask/tree/main/examples/tutorial>
- Upstream license: **BSD-3-Clause** (© Pallets) — this example documents that
  public, permissively-licensed code and is included here for demonstration.

Open the file in any browser — it is fully self-contained (no network, no assets).

It traces the whole app: the application factory, the request lifecycle, the
auth journey (register → login → session → logout), the blog CRUD journey with
its ownership gate, the data model, the parameter glossary, and a
security/correctness boundaries section. Every claim is anchored to `file:line`
in the flaskr source and was verified against it.
